# UI V2 P1 · 视觉草稿对齐首轮

日期：2026-07-20  
依据：用户批准的「ScolvPet 繁殖者工作台」视觉草稿 + `docs/19-…任务书.md` §10–12  
moodboard：`docs/evidence/ui-v2-p0-gate/2026-07-20/00-视觉草稿-moodboard.jpg`

## 本轮完成

### P1-0 真机 `?` 根因（阻塞级）

`pubspec.yaml` **未声明** `cupertino_icons`。  
CupertinoIcons 依赖 `fontPackage: cupertino_icons`；缺包时 Release 真机几乎所有系统图标显示为 `?`。

- 已加：`cupertino_icons: ^1.0.8`
- `flutter pub get` 已变更 lock

### P1-1 Design Tokens

对齐视觉草稿色板（`ScolvPalette.light` / `IosColors`）：

| Token | 值 |
|-------|-----|
| Background | `#FFF8EF` 奶油白 |
| Surface | `#FFFFFF` |
| Text | `#3A2F29` 深咖 |
| Accent | `#D98B55` 品牌暖橙 |
| Accent soft | `#FBE8D8` |
| Success | `#6B7D6B` |
| Danger | `#E2685B` |

`IosMetrics`：圆角 small/medium/large（8/16/24）；间距 scale 4/8/12/16/24/32。

### P1-2 Shell / 入口

- 底栏：`house` / `paw` / `heart`（线性，选中 fill）
- 工作台右上：AI sparkles + **圆形 mascot 头像**进账号（对齐草稿）
- 问候语优先 **舍名**（与账号页大标题一致）

### P1 核心页

- 繁育：分段 **进度 / 窝次 / 计划**（窝次为看板入口，不重造领域）
- 个体档案：`candidate` → **候选**（`i2BreedingStatusLabel`）

## 验证

```text
flutter analyze          → 0 error（2 pre-existing info）
flutter test widget_test / breeding_wizard / home_overview /
  i2_widget / hamster_care_hub / today_care_queue → 全绿
```

## 未做（下轮）

- 手绘线性 icon 全量替换 PNG 水彩 icon（任务书 icon vocabulary）
- 详情二级 Tab：概览/繁育/健康/谱系/记录 顶栏形态
- 列表筛选 chip 与草稿「全部 24 / 在养 18…」计数形态
- 繁育进度有数据时的分组卡片视觉加厚（空态已可）
- 真机 **Release 重装** 验证 `?` 消失（需 USB/devicectl）
- version  bump `0.0.4`

## 红线

- 未改品种/毛色/波利/遗传权威层
- 未扩 Media / OpenAPI / app_state
- 隔离脏文件（i6/media/pbxproj 等）未并入本轮语义
