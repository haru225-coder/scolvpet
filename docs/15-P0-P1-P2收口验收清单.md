# 15 · P0 / P1 / P2 收口验收清单

> 日期：2026-07-17  
> 分支：`codex/openapi-p1-p2-contracts`  
> 工程封板 tip：见本文件末次 commit（P1/P2 OpenAPI 契约收口 + 生成客户端）  
> 功能收口基线 tip：`30ea1e6` `feat(p2): cross-cattery stud listings and deal ledger`  
> 范围：**迁移部署、契约漂移、可本地验证、移动端构建门禁**——不新增功能切片（蓝牙 T-P2-04 明确跳过）

关联：

- P0 交接：`docs/14-全量逆向R13交接.md`
- 对标矩阵：`docs/13-熊舍管家全量对标差距与实现清单.md`
- 本地命令：`docs/engineering/本地开发.md`

---

## 1. 收口结论

| 项 | 状态 | 说明 |
|---|---|---|
| 迁移链 0000–0026 空库可应用 | **已验证** | `make db-verify`（本机 PG 15） |
| 基线表/枚举计数 | **已验证** | public **58** 表 / **75** 枚举；meta 1；system rules 1 |
| `migration-drift`（0001–0009） | **已验证** | 与 `specs/database/schema.sql` 一致 |
| OpenAPI lint（I1–I6 + P1/P2 契约） | **已验证** | Redocly 1.34.5 通过 |
| API 单测 | **已验证** | `make api-test` 全绿 |
| Flutter 单测 | **已验证** | `flutter test` 全绿（94） |
| Flutter analyze | **已验证** | 移除 `public_site_pages.dart` 未使用 import 后无 issue |
| Android debug APK | **已验证** | 启用 core library desugaring 后 `flutter build apk --debug` 成功 |
| 生成客户端 drift | **已验证** | 1035 files（含 P1/P2；OpenJDK 17 下 `make client-drift` 可复跑） |
| 全量 `make ci` | **已验证** | 本机 `JAVA_HOME=openjdk@17` 下全绿 |
| 工程封板（构建门禁） | **已封板** | 两处移动端 CI 红灯已清；最新代码可出 debug APK |
| P1/P2 写入 OpenAPI + regenerate client | **已验证** | 新增 65 个操作、92 个契约 schema；保留手写 repository 作为迁移债务 |

### 1.1 最终定级（复核对齐）

| 维度 | 定级 |
|---|---|
| 功能交付 | **超额完成**（R0–R13 证据 + 全 P0 + 8×P1 + 4×P2；仅 BLE 跳过） |
| 逆向证据 | **按编号例外完成**（FairPlay 下 R4–R8 仍为 B，≠ 完整动态逆向） |
| 工程收口 | **已封板**（`make ci` 绿；analyze + Android 构建修复） |
| 发布状态 | **最新代码可构建 debug APK**；生产签名/真环境迁移仍按 §6 |
| OpenAPI 全量 | **已封板**（86 个既有 path 模板 + 47 个 P1/P2 新模板；新增 65 个操作，总计 173 个操作） |

---

## 2. 迁移部署清单

### 2.1 策略

| 区间 | 来源 | 部署注意 |
|---|---|---|
| `0000` | 元数据 | 必须先于全部 |
| `0001`–`0009` | `tools/split-schema.mjs` ← `specs/database/schema.sql` | `make migration-drift` 门禁 |
| `0010`–`0017` | 手写 I2–I6 增量 | 不可用 split 覆盖 |
| `0018`–`0026` | 手写 P1/P2 增量 | 同上 |

部署命令（新环境）：

```bash
DATABASE_URL='postgres://…' db/scripts/migrate.sh --seed   # 开发种子可选
# 或 CI：
make db-verify
```

### 2.2 文件 ↔ 能力

| Migration | 能力 | 波次 |
|---|---|---|
| 0010–0011 | I2 约束 / 导入上传 | I2 |
| 0012 | auth 持久化 | I1+ |
| 0013–0015 | 繁育 / 窝仔 / 健康任务 | I3–I5 |
| 0016–0017 | 媒体分享 / 数据中心 | I6 |
| **0018** | organization_member RBAC | **P1** |
| **0019** | CRM contact / reservation / handover | **P1** |
| **0020** | 合同 / 回执模板与单据 | **P1** |
| **0021** | 记账分类与流水 | **P1** |
| **0022** | 遗传档案 | **P1** |
| **0023** | 推送设备与消息日志 | **P1** |
| **0024** | 公开官网 public_site | **P2** |
| **0025** | 小程序配置与发布流水 | **P2** |
| **0026** | 跨舍借配 listing / deal | **P2** |

### 2.3 空库基线对象（0026 后）

- **public 表 58**：含 `organization_member`、`crm_*`、`doc_*`、`accounting_*`、`genetic_profile`、`push_*`、`public_site`、`miniprogram_*`、`stud_*` 等  
- **枚举 75**  
- **schema_migrations 行数 27**（0000–0026）

`db/scripts/verify-fresh-postgres.sh` 硬断言已与上述计数对齐；**后续再加迁移时必须同步改表/枚举期望值**。

---

## 3. HTTP 路由清单（实现真源）

注册入口：`api/internal/httpapi/server.go` → `registerI*` / `registerP*`。

### 3.1 规模

| 层 | HandleFunc 约计 | OpenAPI 契约 |
|---|---|---|
| I1 认证/账号/规则 + health | ~13 | 有 |
| I2–I6 主闭环 | ~90 | **86 个 path 模板 / 108 个操作**（servers base `/v1`） |
| **P1** | **41 个操作** | **28 个 path 模板 / 41 个操作** |
| **P2** | **24 个操作** | **19 个 path 模板 / 24 个操作** |
| 合计实现 | **~168** | **133 个 path 模板 / 173 个操作** |

### 3.2 P1 路由

| 模块 | 方法与路径 |
|---|---|
| 成员 | `GET/POST /v1/organization-members` · `PATCH …/{member_id}` · `POST …/{member_id}/revoke` |
| CRM | `GET/POST /v1/crm/contacts` · `GET/POST /v1/crm/reservations` · `POST …/confirm|cancel` · `GET/POST /v1/crm/handovers` · `POST …/complete` |
| 合同/回执 | `GET/POST /v1/contracts[/templates]` · `POST …/{document_id}/issue` · receipts 对称 |
| 记账 | `GET/POST /v1/accounting/categories|records` · `GET /v1/accounting/summary` |
| 遗传 | `GET /v1/genetic/loci|profiles` · `POST /v1/genetic/profiles|simulate` |
| 推送 | `GET/PUT /v1/push/devices` · `DELETE …/{device_id}` · `GET/POST /v1/push/messages` |
| 权益 | `GET /v1/entitlements/catalog|current` · `POST …/check` · `POST …/sandbox/activate` |

### 3.3 P2 路由

| 模块 | 方法与路径 |
|---|---|
| 官网 | `GET/PUT /v1/public-site` · `POST …/publish|unpublish` · `GET /v1/public/sites/{slug}` |
| 小程序 | `GET/PUT /v1/miniprogram/config` · `GET/POST /v1/miniprogram/releases` · `POST …/{id}/submit|audit|publish|rollback` |
| 助手 | `POST /v1/assistant/ask` · `GET /v1/assistant/capabilities` |
| 借配 | `GET/POST /v1/stud/listings` · `POST …/unpublish` · `GET/POST /v1/stud/deals` · `POST …/{id}/confirm|start|complete|cancel` |

### 3.4 Flutter 功能目录（手写 API 客户端）

`apps/mobile/lib/features/`：

| 波次 | features |
|---|---|
| P0 / I* | `shell` `i2` `i6` `breeding` `litter` `weight` `health` `tasks` `calendar` `pedigree` |
| P1 | `members` `crm` `contracts` `accounting` `genetic` `push` `paywall` `home_widget` |
| P2 | `public_site` `miniprogram` `assistant` `stud` |

生成客户端 `generated/dart/scolvpet_api` 已覆盖 OpenAPI I1–I6、P1、P2 全部 173 个操作；P1/P2 业务 repository 暂仍手写，后续可按模块渐进替换。

---

## 4. 已知漂移与债务

| ID | 项 | 严重度 | 处理 |
|---|---|---|---|
| ~~D-OA-P12~~ | OpenAPI 未描述 P1/P2 65 条操作 | — | **已关闭**：补齐 65 个操作、92 个 schema，重新生成 Dart client 并通过 drift |
| D-MIG-SCOPE | `migration-drift` 只覆盖 0001–0009 | 低 | 设计如此；0010+ 靠 review + `db-verify` |
| D-BLE | T-P2-04 蓝牙称重 | — | **本轮跳过**（需真机 BLE） |
| D-FAIRPLAY | 宠舍 R4–R8 动态逆向 | — | FairPlay 阻断下以 **B** 编号收口；≠ 完整动态逆向 |
| ~~D-ANALYZE~~ | `public_site` unused import | — | **已修**（移除 `i2_models.dart`） |
| ~~D-DESUGAR~~ | Android desugaring | — | **已修**（`isCoreLibraryDesugaringEnabled` + `desugar_jdk_libs:2.1.4`） |

### 4.1 封板前两处移动端修复（2026-07-17）

1. **`apps/mobile/lib/features/public_site/public_site_pages.dart`**  
   删除未使用 `import '../i2/i2_models.dart'` → `flutter analyze` 无 issue。

2. **`apps/mobile/android/app/build.gradle.kts`**  
   为 `flutter_local_notifications` 启用 core library desugaring：
   - `compileOptions.isCoreLibraryDesugaringEnabled = true`
   - `coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.4")`
   - `multiDexEnabled = true`  
   → `flutter build apk --debug` 产出最新 P1/P2 代码 APK。

---

## 5. 本机验收命令（可复跑）

```bash
cd /Users/snowchan27/Documents/scolvpet
export JAVA_HOME="$(/usr/libexec/java_home -v 17 2>/dev/null || echo /opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home)"
export PATH="$JAVA_HOME/bin:$PATH"

make migration-drift          # 0001–0009
make openapi-lint             # I1–I6 OpenAPI
make db-verify                # 空库 0000–0026 + 58/75
make api-test
make flutter-analyze
make flutter-test
make flutter-build-android    # 最新 debug APK
make client-drift             # 需 Java + OpenAPI Generator
make ci                       # 全量门禁
```

Debug APK：`apps/mobile/build/app/outputs/flutter-apk/app-debug.apk`

### 5.1 本轮实测（2026-07-17）

| 命令 | 结果 |
|---|---|
| `make migration-drift` | pass（9 source migrations） |
| `make openapi-lint` | pass |
| `make db-verify` | pass（修计数 58/75 后） |
| `make api-test` | pass |
| `flutter test` | pass（+94） |
| `make client-drift` | pass（1035 files，含 P1/P2，复核补跑） |
| `make flutter-analyze` | pass（unused import 修复后） |
| `make flutter-build-android` | pass（desugaring 修复后） |
| `make ci` | **pass**（OpenJDK 17） |

---

## 6. 部署检查表（真环境）

- [ ] 备份现网库  
- [ ] 按序应用至 **0026**（`migrate.sh`，勿手改已应用 checksum）  
- [ ] 确认 `scolvpet_meta.schema_migrations` 含 0000–0026  
- [ ] 冒烟：`/healthz` `/readyz` + 登录会话  
- [ ] 抽测 P1：`/v1/crm/contacts`、`/v1/accounting/summary`、`/v1/entitlements/current`  
- [ ] 抽测 P2：`/v1/public-site`、`/v1/assistant/capabilities`、`/v1/stud/listings`  
- [ ] 对象存储 / Outbox / SMS 按生产 env 配置（见 `.env.example`）  
- [ ] Flutter 指向正确 `basePath`；P1/P2 手写 client 与后端同版本发布  

---

## 7. 波次完成度（产品视角 · PARTIAL 诚实标注）

| 波次 | 服务端+迁移 | Flutter 垂直切片 | 契约/ codegen | 真环境 |
|---|---|---|---|---|
| I1–I6 | DONE | PARTIAL→P0 主路径 | OpenAPI 86 | 视环境 |
| P0 客户端 | — | DONE（T-P0-01…08） | 消费生成 client | 视环境 |
| P1 | DONE（垂直） | DONE（hub/widget 级） | **已进 OpenAPI + codegen** | 待 |
| P2（除 BLE） | DONE（垂直） | DONE（hub 级） | **已进 OpenAPI + codegen** | 待 |
| T-P2-04 BLE | — | **跳过** | — | — |

「垂直切片」= 迁移 + handler + 内存/集成测 + Flutter hub 页；**不等于**宠舍对标的全量经营闭环或生产付费。

---

## 8. 下一步建议（封板后可选）

1. **手写 repository 渐进迁移**：按 P1/P2 模块逐步切换到生成 client，保持现有 API 行为不变。  
2. **真环境验收**：按 §6 勾选；生产签名替换 debug signing。  
3. **BLE**：有硬件后再开 T-P2-04。  
4. **文档 13 矩阵刷新**：把 DEFER 的 P1/P2 行改为 PARTIAL/DONE（可选）。  
5. **FairPlay 解除后**：补宠舍 R4–R8 动态逆向（B → D）。  

---

## 9. 知识库留痕

- 收口文档：本文件  
- 开发日志：`开发日志/2026/07/2026-07-17 · 熊舍管家P0-P1-P2收口验收.md`  
- 封板补记：`开发日志/2026/07/2026-07-17 · 熊舍管家工程封板移动端两修复.md`  
- rsync LA1：`/root/ScolvAtom/`
