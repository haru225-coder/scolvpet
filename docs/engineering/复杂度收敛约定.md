# 复杂度收敛约定

状态：已写入（2026-07-20）。**现有代码不要求一次性整改；新代码必须遵守。**

本约定来自 ponytail-review 后的执行合同：诊断（Flutter 重复模型/仓储、Go 双架构）保留，处方收成「先砍重复与空转，不砍业务边界」。

## 执行原则

只删除**没有独立语义**的层。

| 优先删 | 默认留 |
|---|---|
| 纯转发、字段 1:1 映射、重复状态模板 | 事务 / 权限 / 不变量 / 跨资源编排 / 幂等 |
| 测试专用实现进入生产树（`lib/`） | 测试仍可写（mock 或 `test/` 旁 double） |
| 仅以「将来可能换实现」为理由的抽象 | 已有第二入口或第二实现后再抽 |

判断口令：

1. 删掉这一层后，业务规则是搬家还是消失？
2. 删掉后，事务 / 幂等 / 测试是否更难表达？

答不好就不动。

## API 单轨

> 新增 API 默认采用 `handler → store/SQL`。仅当存在跨资源事务、复杂业务不变量、幂等编排或多入口复用时，才引入独立 service / application 层。禁止仅为「分层完整」新建 `*core`。

说明：

- 现有 `i2core` / `i3core` / … 不要求立刻拆掉；**停止继续扩**。
- 新接口优先学 `p1_*`：在 `httpapi` 中校验权限与入参，经 `store` 或直接 SQL 完成读写。
- 需要 service 时写明理由（跨资源事务 / 不变量 / 多入口），不要默认复制 `*core` 模板。

## Flutter：DTO 与 API 契约冻结

新 feature **禁止**无业务语义的手写 DTO 镜像：

```text
GeneratedDto  →  FeatureModel  →  ViewModel   // 禁止空转
```

允许的中间模型必须增加业务语义（校验、展示派生、领域不变量），不能只是字段拷贝。

优先：

- 页面 / controller 直接使用 `scolvpet_api` 生成类型；
- 展示差异用 `extension`，不重写平行 `*models.dart` 字段表。

既有 `*models.dart` / `client.dio` repository 不强制迁移。**冻结的是平行契约面扩大**，不是禁止一切维护：

- 不得用旧手写路径承接**新** endpoint，或复制为新 feature 模板；
- 既有 endpoint 的兼容性读取修复允许，但不得借机拓宽镜像层；
- gen 不足时先补 OpenAPI 再生成，不在 app 再写一套 HTTP 契约；
- direct HTTP 仅限预签上传等传输例外，或已记录的未覆盖 endpoint。

分类按**类型/契约**，不按「删掉整个 models 文件」。权威摘要见仓库根 [`AGENTS.md`](../../AGENTS.md)「API 契约冻结」。

## Flutter：Memory / Fake

- 测试专用 `Memory*` / Fake **不得**放在 `apps/mobile/lib/` 生产树。
- 放在 `apps/mobile/test/`（推荐 `test/support/`），或测试文件内私有 double。
- 生产路径上的可选适配器（如 SharedPreferences / 通知插件不可用时的内存后备）可以留在 `lib/`，但须有独立语义（「无插件时的本地后备」），不得借测试 double 之名堆第二套完整 Repository。

验收：`lib/` 中不再为「方便测试」维护第二套 `Memory*Repository`；测试仍然容易写。

## Flutter：AppServices

目标不是 Service Locator，而是把根组件构造参数爆炸收成显式依赖束：

```dart
// 期望
ScolvPetApp(services: AppServices(...))

// 避免继续增长
ScolvPetApp(
  authRepository: ...,
  petRepository: ...,
  crmRepository: ...,
  // 20+ 参数
)
```

依赖仍在 `main` 中显式构造并注入；`AppServices` 只是记录类型 / 参数对象。

## 执行顺序（冻结 → 清理 → 试点）

1. **冻结复杂度增长**：本文件 + `AGENTS.md`。
2. **清理明显重复**：Memory 出 `lib/`、`AppServices` 收束（`f7ad414`）。
3. **纯机械拆文件**：`screens.dart` / `i2_core_handlers.go` 等只移代码（`4a5425f`–`846b350`）。
4. **薄模块试点**：`push` 死接线清理（`8197c48`）——见下节结论。
5. **规模化时**：按调用链逐条消灭空转，**不以 feature 五件套为单位批量砍**。

每步可独立回滚。迁移时发现旧代码很丑也先别顺手修。

## 删层推广规则（push 试点沉淀 · 2026-07-20）

证据：`8197c48`。`PushSettingsPage` 只读静态说明；根 DI 挂着 `pushRepository` 无人消费；`PushController` 无 UI 消费者。删 controller + 根 DI 后无逻辑搬家、测试不变难。`PushRepository` 仍对应真实 `/v1/push/*`，故保留。

> **有限 promote：只删除无消费者、无业务规则、无真实调用路径的死接线。只要仍承载真实 API 边界或存在第二入口，就默认保留。**

`push` **不是**「删 repository / 拆五件套」的模板；它是**死代码/死接线清理**模板。

### 下一模块先画调用链

```text
UI / Job / Other entry
        ↓
Controller?
        ↓
Repository?
        ↓
DefaultApi / Store
```

逐层问：有没有真实消费者？有没有独立语义？删掉后逻辑会不会搬家？

只有**同时**满足才直接删：

```text
无真实消费者
+ 无业务语义
+ 无事务 / 幂等 / 状态编排
+ 删除后测试不变难
```

试点提交说明固定四项：`Before/After call chain`、`Test impact`、`Business-rule movement`、`Decision: promote / do not promote`。

## 第 1+2 步验收（不数行数）

| 项 | 通过标准 |
|---|---|
| DTO | 新 feature 无 `Gen → FeatureModel → ViewModel` 空转；中间模型必须带业务语义 |
| Memory | `lib/` 无测试专用第二套仓库；测试仍可写 |
| AppServices | 根组件依赖显式收束，不是隐藏式 Service Locator |
| API 单轨 | 新 PR 不新增仅为分层完整的 `*core` |
| 删层 | 只砍死接线；不批量删仍有 API/第二入口的 repository |

## 明确不做（本阶段）

- 不批量删除 repository 接口 / service 层
- 不强制页面全面改 `FutureBuilder`
- 不把 CSV 导入改成外部脚本
- 不替换自研 objectstore / 不删除 outbox
- **不以 push 为模板推广「删 repository」**

以上项需**逐用例**审查后再动。
