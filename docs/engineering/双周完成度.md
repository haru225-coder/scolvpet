# 熊舍管家 · 双周完成度计分板

> 维护规则：只按已运行测试、可操作演示链和规格覆盖计分；文件数量、静态接线和未运行的构建不计分。每个数字都必须能在本文件的证据链接或验证报告中复核。

## 本轮结论（2026-07-27）

- 起始计分：**108 / 200**（由 2026-07-26 全功能收口审计的可验证项重建；审计结论为 FULL CLOSURE GATE：FAIL，未验证项不计分）。
- 本轮新增：**+8**。
- 当前计分：**116 / 200**。
- 当前状态：**已写入 / 已验证 / 待确认**。

本轮范围收敛为：敏感 GET 的 viewer/unknown 角色隔离、助手 confirm 直写权限回归确认、OpenAPI → Dart 生成客户端漂移收口、Flutter/SSR/迁移/Outbox/媒体与公开预订演示链复验。未新增业务领域或改变冻结领域模型。

## 计分明细

| 维度 | 上轮 | 本轮 | 证据 | 未完成/扣分原因 |
|---|---:|---:|---|---|
| 规格、原型与页面/动作追踪（0-12） | 12 | 12 | [`validation/mvp-spec-validation-report.txt`](../../validation/mvp-spec-validation-report.txt)、[`docs/05-熊舍管家-MVP-产品需求文档.md`](../05-熊舍管家-MVP-产品需求文档.md)、[`docs/08-熊舍管家-MVP-开发排期.md`](../08-熊舍管家-MVP-开发排期.md) | 规格静态覆盖已验证；真实双端手工走查仍未全量完成 |
| 核心业务状态与 AC-01～AC-26（0-40） | 22 | 22 | [`validation/i1-engineering-validation-report.txt`](../../validation/i1-engineering-validation-report.txt)、[`scripts/i1-smoke.sh`](../../scripts/i1-smoke.sh)、[`scripts/public-reservation-smoke.sh`](../../scripts/public-reservation-smoke.sh) | Wave2 非 happy-path、健康纠错、模拟历史实体链、导入 session 持久化仍待做 |
| API、数据库、owner 隔离与事务（0-28） | 24 | **27** | [`api/internal/httpapi/rbac.go`](../../api/internal/httpapi/rbac.go)、[`api/internal/httpapi/rbac_test.go`](../../api/internal/httpapi/rbac_test.go)、[`db/scripts/verify-fresh-postgres.sh`](../../db/scripts/verify-fresh-postgres.sh)、本轮 `db-verify`/I1 smoke/公开预订 smoke | 本轮新增 viewer 敏感读与 unknown role 失败闭合；token 明文幂等记录、历史复合外键和 staging 落后仍待收口 |
| 双端可用性、SSR、导入/媒体/数据中心（0-20） | 12 | **14** | Flutter analyze/test、临时卷 iOS Simulator build、Web test/build、[`scripts/i6-media-smoke.sh`](../../scripts/i6-media-smoke.sh) | Android SDK 缺失；导出/备份 worker 仍按 Wave0 隐藏；S3/MinIO live 未接入 |
| 安全与角色能力（101-115） | 8 | **10** | [`api/internal/httpapi/rbac_assistant_test.go`](../../api/internal/httpapi/rbac_assistant_test.go)、[`apps/web/src/server.mjs`](../../apps/web/src/server.mjs)、Web 媒体 `nosniff` 回归 | XFF 信任、idempotency 中敏感令牌、邀请显式接受仍待收口 |
| 迁移、幂等、Outbox、契约漂移（116-135） | 22 | **24** | `make db-verify`、`scripts/i1-outbox-test.sh`、`make openapi-lint`、`make openapi-conformance`、`make migration-drift`、`make client-drift`、`make ts-client-drift` | 生产 staging 迁移/部署和容量/性能固定场景未验证 |
| 发布、安装升级、备份恢复与可观测（136-150） | 1 | 1 | [`validation/i1-engineering-validation-report.txt`](../../validation/i1-engineering-validation-report.txt) | Android/真机安装、生产域名/签名、备份恢复演练均待确认 |
| 逆向报告高价值增强（151-200） | 7 | 6 | [`reverse-evidence/README.md`](../../reverse-evidence/README.md)、`apps/mobile` genetic/pedigree 测试与 2026-07-25 日志 | 已有谱系/遗传/小程序增强；不再以新增模块堆分，模拟历史链和运营闭环待规划 |
| **总计** | **108** | **116** | 本文件列出的可复核证据 | 仍未达到发布门与完整闭环门 |

## 本轮已验证

1. `GOCACHE=/private/tmp/scolvpet-gocache go test ./... -count=1`（受控环境）通过；API 全包通过。
2. `make db-verify` 通过：全新库、迁移复跑、种子重放、checksum 漂移检测；45 条迁移、78 张表、75 个枚举。
3. `I1_API_PORT=18580 I1_DB_PORT=56580 scripts/i1-smoke.sh` 通过：owner isolation、幂等、ETag、rules、events=4、published_outbox=2。
4. `scripts/i1-outbox-test.sh` 通过：claim、retry、dead-letter、stale recovery。
5. `make openapi-lint`、`make openapi-conformance`、`make migration-drift`、`make ts-client-drift` 通过；Dart 客户端重新生成后 `DART_PUB_OFFLINE=1 make client-drift` 通过（1258 files）。
6. Flutter `analyze` 通过；全量 `flutter test` 通过（216 tests）；在 `/private/tmp` 无 Finder 扩展属性临时卷完成 `flutter build ios --simulator --no-codesign`，生成 `Runner.app`。
7. Web `npm test` 16/16、lint、build 通过；小程序旧版测试通过；小程序 next 38 passed、1 skipped，lint 通过。
8. `make reservation-smoke`、`make media-smoke` 通过；`make objectstore-smoke` 本地 suite 通过。
9. RBAC 角色矩阵新增 viewer 敏感读拒绝与 unknown role GET 失败闭合；助手 confirm 的直接路由守卫已有回归并纳入本轮复验。

## 待确认与阻断

- Android debug：本机未安装 Android SDK，`ANDROID_HOME`/`ANDROID_SDK_ROOT` 为空，`flutter build apk --debug` 明确失败；安装 SDK 后需重跑。
- iOS 工作区本地卷：Finder 元数据会在 `build/` 重新注入并阻断代码签名；临时卷构建已通过，工作区本地产物仍待确认。
- S3/MinIO live：缺少 `OBJECT_STORE_ENDPOINT`、bucket、region、access key、secret key；本地 object store 已验证。
- 真实生产集成：LA1 staging 迁移/二进制部署、正式微信 AppID/OpenID、短信供应商、域名/CDN、签名与真机安装升级未在本轮运行。
- 产品闭环：CSV session 全库化、繁育非 happy-path UI、模拟历史实体、健康纠错、导出/备份 worker 与恢复演练仍在下一窗口。

## 下一双周建议

1. 先补 Android SDK 与正式 staging 部署，跑双端 Release/E2E 及迁移升级回滚。
2. 在不开放新模块的前提下，收口 CSV session 持久化、健康纠错和导出/备份可恢复链。
3. 再处理 token 明文幂等记录、XFF 信任边界、历史复合外键清单和邀请显式接受；每项都补行为级测试。
