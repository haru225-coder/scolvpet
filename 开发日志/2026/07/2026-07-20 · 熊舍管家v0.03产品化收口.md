# 2026-07-20 · 熊舍管家 v0.03 产品化收口

状态：**已验证（自动化）** / **待确认（真机视觉）**

## 目标

承接 Codex 会话 `019f7a47-8859-7230-bc39-b7932c34b48c`（完善熊舍管家至 0.03），把“完成实现”和“自我验收”作为同一交付标准，收口并提交 v0.03。

## 已写入

- 版本号：`apps/mobile/pubspec.yaml` → `0.0.3+3`
- 产品化任务书：`docs/18-熊舍管家-v0.03-产品化收口任务书.md`
- Flutter 全模块状态/权限/空态/错误态收口（仓鼠、笼舍、批量称重、任务、日历、导入、繁育、窝次逐只分性、谱系、借配、CRM、合同、财务、管家、入口与套餐边界等）
- 服务端：RBAC 主体、P1/P2/P3 增长与遗传扩展、成员与合同/CRM 能力门禁
- 公开 Web SSR 获客页与视觉 fixture
- OpenAPI / 生成 Dart 客户端同步；生成物尾随空格清理与 `tools/prepare-generated-dart.mjs` 全树剥离
- 窝次状态机：带崽期不开放断奶，仅 `weaning_due` 可推进；分性改为逐只选择
- 测试对齐产品语义（litter / stud / pedigree / import / session 契约等）

## 已验证

| 门禁 | 结果 |
|---|---|
| `go test ./...`（api） | 通过 |
| `npm test` / `npm run lint` / `npm run build`（apps/web） | 通过 |
| `make migration-drift` | 通过 |
| `make openapi-lint` | 通过 |
| `git diff --check` | 通过（生成物尾随空格已清） |
| `flutter analyze`（apps/mobile） | 0 issue |
| `flutter test`（apps/mobile） | 全量通过（见当次终端输出） |
| `dart format --set-exit-if-changed` | 已格式化 |

## 待确认

- 真机 / 模拟器：浅色、深色、窄屏、1.3 倍字体视觉走查
- 生产凭据下的获客/推送/小程序外部链路
- `make client-drift` 在本机全量重生成耗时较长，依赖生成脚本 JAR 离线路径；当前工作区生成产物已与 OpenAPI 同步并做过空白清理
- LA1 知识库 rsync 需在提交后按知识库规则同步

## 回滚

```bash
git revert <本提交 SHA>
# 或切回
git checkout codex/openapi-p1-p2-contracts~1 -- .
```

版本回退时将 `apps/mobile/pubspec.yaml` 的 `0.0.3+3` 一并还原。
