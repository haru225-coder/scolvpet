# 2026-07-27 · ScolvPet 小程序开发环境一键登录

## 目标

解除 B 端小程序真机验收对微信扫码/真实微信身份的依赖，让开发机可以直接进入全量经营功能，同时保留正式环境的真实登录链路。

## 已写入

- `apps/miniprogram-next/src/pages/login/index.tsx` 新增“开发环境一键登录（免扫码）”。
- 入口调用真实 staging `sendVerificationCode`，再使用开发 Mock 码 `123456` 调用真实 `createSession`，保存 Bearer 会话后跳转今日页；手机号统一按契约转换为 `+86...`。
- 默认使用 `13800138000` 演示舍主一；登录页预填其他测试手机号时沿用该手机号。
- 入口由 `APP_ENV=development` 控制，生产配置生成后不显示。
- `scripts/build-miniprogram-next.sh` 已同步开发配置的生成/恢复逻辑，避免 `--restore` 丢失开发常量。
- README 增加开发者工具使用说明。

## 验证

- **已验证**：`npm exec tsc -- --noEmit`。
- **已验证**：`npm run lint`。
- **已验证**：`npm test`，38 tests passed，1 skipped。
- **已验证**：`node --test test/release_gate.test.mjs`，4 tests passed；生产配置不带开发手机号/Mock 码，开发配置可完整恢复。
- **已验证**：`CI=1 npm exec taro -- build --type weapp --pages pages/login/index --no-check`，登录页定向产物编译成功，并在 `dist/pages/login/index.js` 中确认一键登录代码已生成。
- **已验证**：staging `/readyz` 返回 ready、`sms_provider=mock`；使用 `+8613800138000` 实际完成验证码申请和演示舍主一 owner 会话创建，令牌输出已隐藏。
- **待确认**：全量 `npm run build:weapp` 仍触发 Taro 依赖的 macOS `system-configuration` worker panic，进程未进入 Webpack 阶段后已停止；需在微信开发者工具或稳定构建环境重新编译全量分包后导入。

## 使用

微信开发者工具导入 `/Users/snowchan27/Documents/scolvpet/apps/miniprogram-next`，打开登录页点击“开发环境一键登录（免扫码）”。登录成功后，业务页面继续访问 staging 真实 API，不切换到静态假数据。

## 状态

已写入 / 已验证 / 待确认（全量小程序产物构建工具链与真机业务链路）。
