# 2026-07-31 · ScolvPet 小程序全功能迭代审查与任务

## 目标

对 `apps/miniprogram-next` 做全量静态审查，并将全部已声明功能拆为可验收的迭代任务；本次不修改业务实现。

## 已写入

- 新增任务书：[2026-07-31-miniprogram-full-iteration.md](../../../docs/superpowers/plans/2026-07-31-miniprogram-full-iteration.md)。
- 任务书清点 `src/app.config.ts` 的 37 个页面入口：B 端主包 2 页、B 端 10 个分包 28 页、C 端原生兼容 7 页；路由覆盖脚本复核为 37/37。
- 任务按基线、登录与 C 端、个体与窝次、繁育与提醒、CRM/合同/财务、遗传/数据中心/AI、逐页真机、发布 Gate 八个任务拆分。
- 每页统一采用进入与返回、生成契约、权限会话、写入安全、状态完整性、交互视觉、回归证据七项验收卡。

## 已验证

- `npx tsc --noEmit`：通过。
- `npm test`：14 个测试文件、60 通过、1 跳过；首次并行运行时读取到 SHA-256 常量修正的中间版本，静止后顺序重跑通过。
- `npm run test:native`：22/22 通过。
- `npm run lint`：退出码 0；仍有 124 条 `no-explicit-any` 警告，已列入 Task 1 与各领域的收敛约束。
- 已核对 B 端直接 `Taro.request` 仅用于预签二进制上传，C 端 `utils/api.js` 仅服务原生兼容页。

## 待确认

- 37 页缺少完整的逐页真机证据；不能仅凭“已写入”声明全功能已验证。
- `make miniprogram-next-build` 与 `make release-miniprogram-next` 未在本次运行，以避免覆盖并行工作树的 `dist/`；列入静止工作树后的 Task 8。
- 微信正式 AppID、合法域名（含对象存储上传/下载 origin）、订阅模板、真实短信/微信通道、客户双机与低端 Android 真机回归均为工程外前置。

## Task 1 执行记录

### 已写入

- `tests/feature-iteration-matrix.test.ts` 与 `tests/support/declared-routes.ts`：从真实 `app.config.ts` 读取主包/分包路由，强制 37 个路由各有领域、关键路径、自动化测试映射和真机证据位置。
- `tests/migration-surface.test.ts`：B 端 `Taro.request`/`wx.request` 只允许 adapter 与个体头像、数据中心 CSV 的预签二进制上传；扫描会排除注释，避免 `client.ts` 的说明文字造成误报。
- `test/lint_baseline.test.mjs`：冻结 lint 预算为 124 条 warning；新增 warning 或任意 error 会让原生测试失败，减少存量 warning 不受影响。
- `docs/evidence/miniprogram-full-iteration/README.md`：写入逐页真机证据表与 B/C 端出口判据。
- `pages/today/index.tsx`：移除没有 OpenAPI 延期端点支撑、只会弹提示的“顺延到明天”伪操作；跳过面板仅保留真正调用 `cancelTask` 的动作。

### 已验证

- 新覆盖矩阵测试：1/1 通过；迁移面与直连 HTTP 门禁：6/6 通过；今日页伪操作回归：7/7 通过。
- 全量 Vitest：15 个文件通过、1 跳过，共 62 通过；原生测试 22/22；`npx tsc --noEmit` 通过；lint 无 error、保留 124 条存量 `any` 警告。

### 待确认

- Task 2 仍需补齐 B 端登录/今日的完整错误与权限回归，以及 C 端 7 页的关键路径自动化和客户双机真机闭环。
