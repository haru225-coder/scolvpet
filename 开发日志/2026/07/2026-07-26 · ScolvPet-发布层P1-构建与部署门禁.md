# 2026-07-26 · ScolvPet 发布层 P1 构建与部署门禁

## 目标

按 docs/30 任务书推进发布层：先修小程序 SMS 重发 bug，再完成 P1（发布工程基座，无外部依赖）。

## 完成

- **fix(miniprogram) `21f01f6`**：`detail.js` 重发守卫判错字段（`codeCooldown` vs `codeCountdown`），
  60s 防重发从未生效；`my-reservations` 完全没有守卫。两页统一字段与定时器模式。
- **P1-1 `1db62ff`**（修审计 P1-3.8）：`make release-miniprogram` 构建期注入
  `MP_APP_ENV/MP_API_BASE/MP_APPID`，production 构建 fail-closed 拒绝
  touristappid / p.scolv.com / 显式端口 / 非 https；`--restore` 字节级还原开发默认值；
  4 个新 fixture 进 `miniprogram-test`。
- **P1-2 `c6f4df5`**：compose `env_file` 参数化（`${ENV_FILE:-../../.env}`，staging 不传时行为不变）；
  `.env.production.example` 逐项对齐 config 加载器的 production 校验（含 P2 用 `WECHAT_*` 占位）；
  `scripts/production-readiness-smoke.sh`（HTTP + 静态断言两段，可独立跳过）。

## 验证

```text
make miniprogram-test                      → 6/6 PASS
make release-miniprogram（坏值 4 组）       → 全部非零退出
make release-miniprogram（好值）+ --restore → 产物无 staging 痕迹，还原后 git 无差异
production-readiness-smoke.sh 正/反例      → exit 0 / 1（反例精确报错）
api/cmd/server 只读编译 + 正反例 env        → config 校验与模板逐项对齐
```

**状态：已验证**（本地）。**待确认**：VPS 上 `docker compose config` 语义校验（本机无 docker，仅 YAML 语法校验）。

## 执行方式

P1-1 / P1-2 由两个并行 sub agent 执行（P1-1 因连接错误重试两次后完成），主会话逐项复验后分主题提交。

## 推迟项

- `/readyz` 生产自检增强：handler 在 `server.go`，该文件有并行审计任务未提交改动，待其落地后再做。

## 下一步

- P2 微信身份接线（mock code2Session 下可全部完成，不等 AppID）
- 外部申请（ICP 备案 / AppID 认证 / 短信签名）需用户发起，为关键路径
