# 2026-07-19 · AI 获客闭环 P0 收口

## 背景

Codex 在 `docs/17-熊舍管家-AI获客闭环-P0-实施任务书.md` 执行中断（额度耗尽）。接手时后端/Web/OpenAPI 已有半成品，Flutter 获客层缺失。

## 接手前进度

| 层 | 状态 |
|---|---|
| `db/migrations/0028_ai_growth_p0.sql` | 已有 |
| `api/internal/growthcore/` | 已有（模板/LLM/顾问） |
| `api/internal/httpapi/p3_growth.go` | 已有完整 handler |
| OpenAPI growth 路径 | 已有，`nullable` 写法不合规 |
| `apps/web` SSR `/p/{slug}` | 已有 |
| Flutter `features/growth` | **缺失** |
| 一级 Tab「获客」 | **仍为笼舍** |
| 定向测试 | 极少 |

## 本轮完成

### 后端修正

- 活动 `public_url_path` 改为 `/p/{slug}?campaign={code}`（不再把 code 当 slug）
- 公开页归因接受 `ready|published` 活动（生成后即可带参访问）
- 补强 `growthcore` / `p3_growth` 单测

### Flutter 获客工作台

- 新增 `apps/mobile/lib/features/growth/`（models/repository/controller/pages）
- 一级导航：`今日｜仓鼠｜获客｜繁育｜我的`
- 笼舍保留首页快捷入口与路由返回
- Memory Repository 覆盖 profile → script → link → leads happy path

### Web / 契约

- SSR growth catalog / consult / lead 代理测试
- OpenAPI 3.1 null 类型修正并 `make generate-client`

## 验证（已执行）

```text
make migration-drift   ✅
make openapi-lint      ✅
make client-drift      ✅
make api-test          ✅
make web-lint/test/build ✅
flutter analyze        ✅
flutter test           ✅ 116 passed
```

## 待确认

- `make db-verify`：依赖本地临时 Postgres，本轮未跑
- 真机/联调：需已发布 `public_site` + 迁移 0028 后端到端演示
- 真实 LLM Key：无 Key 时模板路径已验证；有 Key 时 LLM 回退路径仅单元测

## 纪律

- 未 commit / 未 push / 未部署
- 用户既有视觉收口与 genetic 等改动保留

## Ponytail 低风险瘦身复核（续）

### 已写入

- 全仓静态复核：Go `vet`、Flutter `analyze`、Web `lint` 均通过。
- 确认 `apps/mobile/pubspec.yaml` 中 `cupertino_icons` 无源码引用；移除该直接依赖，并由 `flutter pub get` 同步锁文件。
- 未删除用户已有的资源备份、断链 `apps/mobile/build`、生成 Dart client 或业务双实现。

### 已验证

```text
Go test ./...                         ✅
Flutter test                          ✅ 116 passed
Web test/lint/build                   ✅
migration-drift                       ✅
openapi-lint                          ✅
client-drift                          ✅ 1110 files
```

### 待确认

- `apps/mobile/assets/_raw_backup/` 是未纳入版本控制的资源副本，约 3.7 MB；因其可能是用户设计备份，本轮保留。
- 生成 client 的 API 分组、手写 Dio 与生成 API 双轨、genetic 双端逻辑、大页面拆分仍需单独设计迁移窗口，不纳入最小风险级。

## 品种/表型选择器统一（前端修复）

### 已写入

- `IosPickerField` 不再按选项数量在 `CupertinoActionSheet` 与 `CupertinoPicker` 之间分流。
- 品种、表型、系列等选择字段统一使用 iPhone 原生风格底部滚轮，并保留“取消 / 完成”确认栏。
- 新增短列表一致性回归测试，并更新手填选项测试流程。

### 已验证

```text
flutter analyze                       ✅
flutter test test/ios_widgets_test.dart ✅ 4 passed
flutter test                          ✅ 117 passed
```

## 全量前端交互复核（轻量修补）

### 已写入

- 获客资料编辑、脚本生成的 4 个选择字段移除 Material `DropdownButtonFormField`，统一接入 `IosPickerField` 滚轮交互。
- 遗传档案创建弹窗的表型/位点/置信度选择统一接入 `IosPickerField`；修复原 ActionSheet 取消时把表型误回调为 `null` 的问题。
- CRM 三个底部表单返回后增加页面存活判断，避免页面已退出仍继续触发提交。
- 增加获客选择字段的统一滚轮回归测试。

### 已验证

```text
flutter analyze                                      ✅
flutter test test/growth_test.dart test/ios_widgets_test.dart test/genetic_test.dart ✅
flutter test                                          ✅ 118 passed
go test ./...                                         ✅
apps/web npm test                                    ✅ 8 passed
```

### 待确认

- 尚未做真机上的键盘、系统分享、横竖屏和极小屏尺寸走查；本轮先保持低风险，不做大页面拆分和生成 client 迁移。
- 通用 `showIosActionSheet` 工具仍保留给非表单菜单场景；业务选择字段已无 Material 下拉框残留。

## 仓鼠详情页黑金看板重构

### 已写入

- 参考仓鼠详情视觉稿，将详情页重构为近黑背景、暖金强调和低透明度面板的单页滚动看板。
- 新增离线状态条、仓鼠资料卡、健康状态徽章、关键指标、健康概览、最近记录、谱系、护理任务、体重历史和快捷操作区。
- 复用现有仓鼠数据、护理任务、健康记录和体重记录，不改后端契约；保留健康/遗传快捷入口与任务完成回调。
- 保留原有测试定位键，并补充资料卡、健康概览、最近记录和任务区的结构断言。

### 已验证

```text
flutter analyze                       ✅
flutter test test/hamster_care_hub_test.dart ✅ 3 passed
flutter test                          ✅ 118 passed
```

### 待确认

- 真机视觉走查：深色状态栏、头像裁切、不同屏宽、长页面滚动和触控命中区域。
- 当前未 commit / 未 push / 未部署。

## 熊舍真实 Agent V1：数据感知、操作草案与线上模型

### 已写入

- 服务端 Assistant 从“规则回答 + 文案润色”升级为数据感知 Agent：每次提问加载当前用户所属熊舍的概况、仓鼠、笼盒和未完成任务，再交给 OpenAI-compatible 模型规划回答与操作。
- 新增 Agent 操作协议，首批白名单为：任务草案、打开仓鼠详情、打开笼盒详情、任务列表、数据中心和获客工作台；服务端限制最多 3 个动作，并对对象 ID、任务类型、优先级和计划时间做归属与枚举校验。
- 写操作不在对话接口直接执行；`task_draft` 在手机端显示“需确认”操作卡，打开已预填的任务编辑页，由用户确认保存后才调用现有任务 API。
- 手机端 Assistant 支持渲染模型返回的操作卡，并完成仓鼠/笼盒详情定向导航、数据中心/任务/获客页面导航和任务草案预填。
- LA1 的 `scolvpet-api` 已配置内网 Grok2API，模型为 `grok-build-0.1`；保留部署前容器与二进制回滚副本。
- Release 包已通过 CoreDevice 安装到 `Snow’s 16 Pro Max`；自动启动时设备处于锁屏状态，解锁后可直接点应用测试。

### 已验证

```text
go test ./...                         ✅
flutter analyze                       ✅ No issues found
flutter test                          ✅ 125 passed
flutter build ios --release           ✅ Runner.app 30.1 MB
线上 API /healthz                     ✅ healthy
API 容器 → Grok2API /v1/models        ✅ grok-build-0.1
CoreDevice 真机安装                   ✅ cn.scolvpet.dev
```

### 待确认

- 真机解锁后登录，进入“AI”，测试自然语言指令：例如“给某只仓鼠一小时后安排称重”“打开某只仓鼠”“打开数据中心”。
- 确认任务操作卡只进入预填草案，点击“保存任务”后才产生真实任务。
- 当前代码未 commit、未 push；API Agent V1 已部署，移动端 Agent V1 已安装到测试机。

## 真机推送与签名刷新

### 已写入

- 为 `Snow’s 16 Pro Max`（iOS 26.5.2）生成 Release 真机包并覆盖安装 `cn.scolvpet.dev` 0.0.2+2。
- 首次包因本机钥匙串没有有效签名身份被设备拒绝启动；随后通过 Xcode 自动签名刷新 Apple Development 证书和团队描述文件，重新构建并安装。

### 已验证

```text
xcodebuild Release + 自动签名          ✅ BUILD SUCCEEDED
devicectl install                      ✅ App installed
devicectl process launch               ✅ Launched application
```

### 待确认

- 用户在真机完成本轮头像、窄屏笼舍、键盘避让、数据中心重试和 AI 操作验收。

## 真机离线只读与登录恢复修复

### 已写入

- 修正 Release 构建遗漏 `API_BASE_URL` 后回退 `127.0.0.1:8080` 的问题，将默认生产地址设为 `https://p.scolv.com:8443`，真机包仍显式传入相同地址。
- 修正旧 token 失效被误判为离线：仅连接失败与超时读取离线缓存；认证或其他服务端错误清理旧会话并返回登录页。

### 已验证

```text
生产 API TLS/域名可达                    ✅
flutter analyze                         ✅
widget + state safety tests             ✅ 10 passed
flutter build ios --release + dart-define ✅
devicectl install                       ✅
```

### 待确认

- 最终包已安装；自动启动时设备处于锁屏状态，用户解锁后直接打开 Scolvpet 验证登录。

## 前端打磨代码 Review

### 已写入

- 修正头像首字截取，改用 Unicode code point，避免代理对字符被截断。
- 为头像补充图片无障碍语义；网络头像仍保留加载失败回退。
- 调整笼舍网格卡片比例，容纳双入住信息；快照暂时找不到仓鼠档案时显示“档案同步中”，不再误判为空笼。

### 已验证

```text
flutter analyze                       ✅
flutter test                          ✅ 122 passed
```

### 待确认

- 真机检查窄屏下双入住卡片、长姓名和头像裁切。
- 当前未 commit / 未 push / 未部署。

## 前端视觉与空态入口继续打磨

### 已写入

- 新增统一 `HamsterAvatar`：支持头像 URL、加载失败回退、首字/爪印占位和状态色环，可复用于列表、详情与笼舍看板。
- 仓鼠列表改用统一头像组件；笼舍卡片补充入住仓鼠姓名、性别、年龄、最新体重、容量与空笼提示。
- 仓鼠筛选无结果支持一键清除筛选；无仓鼠档案时提供新建入口；无笼盒时提供添加笼盒入口。
- 保持现有黑金主题、状态色和点击下钻行为，未引入新的视觉依赖。

### 已验证

```text
flutter analyze                       ✅
flutter test                          ✅ 122 passed
```

### 待确认

- 真机检查头像裁切、长姓名省略、笼舍卡片在不同屏宽下的高度和触控反馈。
- 当前头像组件已支持 URL 回退，系统相册选择与媒体上传仍待接入媒体 API。
- 当前未 commit / 未 push / 未部署。

## Flutter 交互基础与数据中心重试收口

### 已写入

- 应用根部启用点击空白区域收起键盘；AI Agent 输入框使用发送动作键并在提交前收起键盘。
- 高频表单的 86 个输入控件统一补齐 `TextInputAction`；多行备注保留换行，连续字段使用下一项，末字段使用完成/搜索。
- CRM 联系人、预订、交付弹层改为可滚动并随 `viewInsets` 抬升，避免键盘遮挡底部保存按钮。
- 数据中心接通导出/备份失败任务的真实重试 API，传递版本 `If-Match` 与幂等键；移除数据中心控制器中误导性的“即将开放”提示。
- 增加全局键盘收起回归测试。

### 已验证

```text
flutter analyze                       ✅
flutter test                          ✅ 122 passed
```

### 待确认

- 需要真机检查各表单动作键、弹层键盘避让和数据中心失败任务重试反馈。
- 媒体库与公开分享仍是独立的媒体编排入口，当前页面保留真实待接入状态。
- 当前未 commit / 未 push / 未部署。

## 真机白屏排查与获客初始化时序修复

### 已写入

- 移动端版本提升为 `0.0.2+2`；iOS 构建产物的 `CFBundleShortVersionString=0.0.2`、`CFBundleVersion=2`。
- 将 `SharedPreferences` 的启动读取改为 3 秒有界兜底；存储插件慢或不可用时先使用内存本地存储，避免首帧被插件初始化永久阻塞。
- 真机 Debug 日志确认 Flutter 引擎已完成构建、安装和文件同步；捕获到获客生成页在 `initState` 内同步刷新资料并触发 `setState() or markNeedsBuild() called during build`。
- 将获客生成页的首次资料刷新延后到首帧之后，避免构建阶段通知 `GrowthController`。
- 增加空资料状态直接打开生成页的回归测试。

### 已验证

```text
flutter analyze                       ✅
flutter test test/growth_test.dart    ✅ 7 passed
flutter test                          ✅ 119 passed
flutter build ios --no-codesign       ✅ Runner.app 29.6 MB
CoreDevice 安装并启动 0.0.2          ✅ `cn.scolvpet.dev`
```

### 待确认

- Flutter 无线调试链路仍出现 VM Service 75 秒未重新发现；这次 `flutter run` 失败发生在调试服务发现阶段。设备侧通过 CoreDevice 已完成 0.0.2 安装并发起启动，最终视觉走查建议改用 USB 或允许应用访问本地网络后复测。

## 全量移动端 UI 黑金视觉重构

### 已写入

- 将移动端主主题统一为参考图的近黑底、深层面板、暖金强调和低透明度分隔线；固定使用深色主题，避免随系统浅色模式退回旧视觉。
- 统一全局 AppBar、底部导航、输入框、FilledButton、FloatingActionButton、Card、分组面板、区块标题、图标块、SnackBar 和空态的视觉层级。
- 所有业务页继续复用原有路由、数据模型、控制器和回调，首页、仓鼠、获客、繁育、我的及其下钻页面通过共用 UI 层同步完成换肤。
- 修正金色按钮的深浅文字对比度，并将通用仓鼠素材统一裁为圆形头像，避免浅色方底破坏深色界面。
- 主壳测试增加深色主题与暖金主色视觉契约。

### 已验证

```text
flutter analyze                       ✅
flutter test test/widget_test.dart test/home_overview_test.dart test/ios_widgets_test.dart ✅
flutter test                          ✅ 118 passed
```

### 待确认

- 需要真机逐页视觉走查：iPhone 不同尺寸、刘海/底部安全区、浅色素材在黑底上的最终观感、悬浮按钮触控范围和横竖屏。
- 当前未 commit / 未 push / 未部署。

## AI Agent、任务与笼舍数据闭环

### 已写入

- 将底部中间 Tab 从“获客”调整为“AI”，接入熊舍 AI Agent 对话页；Agent 首屏直接展示在养仓鼠、笼盒、待办/逾期数量，并提供添加任务、任务列表、笼舍数据和获客工作台入口。
- AI 有可用 Grok2API 时默认请求 LLM 润色；服务端无 Key 时自动回退结构化规则回答，回答数字仍来自当前 facts。
- 任务列表新增“添加任务”入口，支持自定义/清洁/用药/随访/称重/建档、关联熊舍/仓鼠/笼盒、优先级、计划时间和备注，并复用现有任务创建 API。
- 笼舍看板新增“添加笼盒”入口，支持编号、笼架、层位、容量和设施，接入现有笼盒创建 API；空置、入住、隔离和清洁状态继续来自真实快照。
- 数据中心不再使用默认空 loader：接入 `/data-center/summary`，并接入真实导出/备份创建接口，摘要显示用量、任务进度和最近备份。

### 已验证

```text
flutter analyze                       ✅
flutter test                          ✅ 121 passed
go test ./internal/aicore ./internal/httpapi ✅
flutter build ios --release           ✅ 0.0.2+2 / 29.8 MB
CoreDevice 安装并启动                 ✅ cn.scolvpet.dev 进程持续运行
```

### 待确认

- 需要在真机上登录后按“AI → 现在有多少只在养？→ 添加任务→ 笼舍数据→ 添加笼盒→ 数据中心”走一遍；当前 CoreDevice 可确认安装和进程，无法代替用户对触控与视觉的逐页确认。
- 当前未 commit / 未 push / 未部署。

## 移动端功能闭环迭代与代码审查

### 已写入

- 仓鼠头像补齐相册选择、SHA-256 校验、媒体预签名上传、对象存储 PUT、完成上传、仓鼠封面关联、上传失败重试和解除封面关联。
- 头像统一在仓鼠列表、仓鼠详情和笼舍看板复用；成功上传的压缩图片字节随 I2 快照持久化，在线刷新按仓鼠与媒体 ID 合并，离线冷启动优先展示本地缓存。
- 数据中心“媒体库”和“公开分享”从占位提示改为真实入口，分别进入仓鼠头像/封面管理和公开主页编辑、预览、发布/取消发布流程。
- AI 回答卡拆为“事实 / 建议 / 操作”三层；任务写入先打开任务草案确认页；仓鼠和笼盒支持基于当前快照选择并打开具体详情，空数据时回退列表入口。
- 增加头像缓存序列化、AI 分层操作与数据中心入口回归测试；AI 长回答列表增加底部滚动留白，避免操作按钮被输入栏遮挡。

### 已验证

```text
flutter analyze                       ✅ No issues found
flutter test                          ✅ 124 passed
flutter build ios --debug --no-codesign ✅ Runner.app
```

### 待确认

- 真机需要走查相册权限、真实对象存储 ETag 返回、上传进度/失败重试、移除头像、冷启动飞行模式头像，以及多仓鼠/多笼盒 AI 选择器。
- OpenAPI 当前没有媒体资产列表和物理删除接口；本轮“媒体库”以仓鼠档案的头像/封面管理为入口，移除头像只解除业务关联并保留原媒体资产。
- 当前未 commit / 未 push / 未部署。

## Assistant 请求超时修复

### 已写入

- 根据线上日志确认：Agent 模型调用本身正常，但手机通用 API client 的 12 秒接收窗口先于模型响应结束，导致服务端记录 `context canceled`，手机显示“助手请求失败”。
- Assistant 请求单独放宽接收超时至 35 秒，发送超时保持 10 秒，不影响其他接口。
- Agent 服务端增加 600 token 上限，减少结构化回答的无效等待；API 已重新部署并保留上一版回滚副本。

### 已验证

```text
flutter analyze                       ✅
flutter test test/assistant_test.dart ✅ 4 passed
go test ./internal/aicore ./internal/httpapi ✅
flutter build ios --release           ✅ Runner.app 30.1 MB
线上 API /healthz                     ✅ healthy
CoreDevice 真机重新安装               ✅ cn.scolvpet.dev
```

### 待确认

- 请解锁手机后重新打开 AI，发送“现在有多少只在养？”；若仍失败，我会根据新的 request_id 继续追踪具体账号请求。

## 离线状态误判修复与真机在线复验

### 已写入

- I2 快照恢复不再把所有异常都归类为离线；只有连接错误、连接/发送/接收超时，以及明确的“请求未完成”网络异常才启用缓存只读。
- 解析错误、权限错误和业务错误现在显示真实错误状态，并保留重试入口，避免错误地禁用写操作。

### 已验证

```text
flutter analyze                       ✅
flutter test test/i2_controller_test.dart test/assistant_test.dart ✅ 6 passed
flutter build ios --release           ✅ Runner.app 30.1 MB
CoreDevice 安装并启动                 ✅ cn.scolvpet.dev
真机在线请求                           ✅ /me、仓鼠、笼盒、窝次、体重、任务、提醒、Assistant 能力
```

### 待确认

- 请在刚启动的版本进入 AI，再发送一次问题；如果仍显示离线，请提供当前页面截图，我会按新的请求日志继续定位。
