# 2026-07-26 · ScolvPet 发布层 P2 微信身份接线

## 目标

docs/30 任务书 P2：wx.login 静默登录 + 短信绑定闭环，mock code2Session 下全部完成，不等正式 AppID。

## 完成

- **P2 scaffold `3f4db7a`**：`api/internal/wechat` provider 接口 + `Server.Wechat` 字段。
  server.go 与并行审计任务共用，采用 hunk 级暂存（`git apply --cached` 过滤行号）只提交本任务两个 hunk。
- **后端 `其后 commit`**：迁移 0040（`customer_wechat_identity` 部分唯一索引保 active openid 唯一、
  解绑置 revoked_at 不删行；`wechat_bind_ticket` 库存 sha256、10 分钟过期一次性）；
  三端点：wechat-sessions（绑定→静默 ct_* session / 未绑定→wt_ 票据）、wechat-bindings（短信验证
  消费票据并绑定）、DELETE wechat-bindings/current（解绑）；code2Session http+mock 双实现，
  production 配置强制 http + AppID/Secret 非空；SessionKey 永不出 handler；IP 频控。
- **小程序 `c911bbf`**：启动 wx.login 三态（绑定发 token / bind_required 存内存 ticket /
  失败静默降级短信）；决策逻辑抽成 `utils/wechat_login.js` 纯函数（wx API 注入）；
  绑定失败自动降级 createCustomerSession 且强制消费 ticket 防死循环；
  detail 与 my-reservations 有 token 时隐藏验证码区块。
- **客户端 `f6d61ec`**：dart-dio client regenerate。

## 验证

```text
go build/vet/test ./...（19 包）        → 全绿
node tools/check-openapi-route-conformance.mjs → PASS（214 ops）
make miniprogram-test                   → 15/15（新增 wechat_flow 9 项）
flutter analyze                         → 零问题
```

**状态：已验证**（mock provider 下）。**待确认**：真实 AppID 下 code2Session 联调、
0040 迁移在带 DB 环境跑 `db-verify`（表数断言已更新 72→74）、微信真机三态流程。

## 执行方式与事故记录

- 后端/小程序两个并行 sub agent 按同一份钉死契约独立开发；后端 agent 连续 5 次 API 断连，
  OpenAPI 契约、httpapi 测试由主会话接手完成。
- **事故**：并行审计窗口在共享仓库 `git add` 了半成品，主会话一次 `git commit` 将其卷入
  （18 文件），已用 `git reset --soft` 拆开重提，内容无损。**此后所有提交改用显式 pathspec
  （`git commit -- <paths>`），与并行窗口共库时必须如此。**

## 下一步

- P3 短信薄适配器（半个窗口）；P4 等 ICP 备案
- 外部申请（备案/AppID/短信签名）仍需用户发起
