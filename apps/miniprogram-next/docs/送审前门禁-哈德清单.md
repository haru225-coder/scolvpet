# 送审前门禁（对齐哈德全量审计）

> 源码默认是 **development**。**禁止**把未注入的源码 zip 直接当微信送审包。

## 哈德已核实修好（代码侧）

| 项 | 状态 |
| --- | --- |
| 离线写接口禁止假 200 | 已：写操作 503 + `OFFLINE_DEV_READ_ONLY` |
| C 端首页 slug 兜底 | 已：`DEFAULT_PUBLIC_SLUG` + 预填「进入熊舍」 |
| 列表空态 | 已：`BListPage` 统一壳 |
| 手填 ID → Picker | 已：繁育/合同/客户/提醒/批量/数据中心等 |
| 订阅不手填 tmpl_ | 已：服务端 ∪ 配置；无模板时隐藏入口 |
| 数据中心高级折叠 | 已 |
| 今日不承诺假优先级 | 已：标题「今天待办 N 件」；副标题仅「例如：任务 · 时间」 |
| tabBar 降级图标 | 已：`assets/tab/*.png` |
| 列表不泄漏 UUID | 已 |
| release 注入痕迹 | 已：`RELEASE_INJECTED_AT` + 生产禁 DEV_LOGIN |

## 哈德清单最后四步（送审执行）

### 1. 今日副标题（代码 · 已收）

副标题只保留「例如：xxx · 时间」，**不再**写「按时间与优先级排好」。

### 2. 订阅入口（代码 · 已收）

- 无可用模板时：**我的 / 日历**不展示「订阅消息」入口  
- 若深链仍进订阅页：显示「暂时开不了到点提醒」，**不说运营/模板 ID**

### 微信后台域名格式（勿再写错）

输入框要求 **以 `https://` 开头**，无端口、无路径、无末尾 `/`：

| 格子 | 内容 |
| --- | --- |
| request | `https://pet.scolv.com`（体验版还打 staging 则另加 `https://p.scolv.com`） |
| downloadFile | `https://pet.scolv.com` |
| 其余 | 留空 |

**4G 公网验 `https://pet.scolv.com/healthz` 通过后再保存**（每月仅 5 次修改）。

### 3. `demo` 熊舍有货（运营 · 待你确认）

`DEFAULT_PUBLIC_SLUG='demo'` 兜底后，正式库必须：

- 存在 slug=`demo` 的公开熊舍，**或**  
- 注入时改掉：发布脚本可另设 `DEFAULT_PUBLIC_SLUG`（若尚未支持，先在正式库备好 demo）

至少：几只在售、有图、能进详情。否则审核员看到空列表。

### 4. 完整仓库跑门禁 + 注入产物（执行 · 不在源码 zip 内）

**说明**：`package-miniprogram-next.sh` 打的是 **源码审阅包**，不含 monorepo 根的 `scripts/build-miniprogram-next.sh`。  
`test/release_gate.test.mjs` 依赖该脚本，**必须在完整 ScolvPet 仓库**里跑，沙箱/仅 zip 跑不通属预期。

```bash
# 在 monorepo 根
cd apps/miniprogram-next && npm ci && npm test
npm run test:native   # 含 release_gate

# 生产注入（正式域名 + AppID + 建议带订阅模板）
MP_APP_ENV=production \
MP_API_BASE='https://正式域名' \
MP_APPID='wx正式appid' \
MP_WECHAT_SUBSCRIBE_TEMPLATE_IDS='tmpl_xxx,tmpl_yyy' \
bash scripts/build-miniprogram-next.sh

# 确认 src/utils/config.js：
#   APP_ENV=production
#   DEV_LOGIN_PHONE / DEV_LOGIN_CODE 为空
#   RELEASE_INJECTED_AT 非空
#   WECHAT_SUBSCRIBE_TEMPLATE_IDS 有值（若要开订阅）

# 再 build:weapp，用 dist 上传微信后台——不要用未注入源码 zip
```

## 内部审包（非送审）

```bash
STAMP=$(date +%Y%m%d-%H%M)
bash scripts/package-miniprogram-next.sh \
  "$HOME/Desktop/ScolvPet-miniprogram-next-内部审-收口-${STAMP}.zip"
```

## 仍属后续（不挡送审）

- 今日任务服务端排序契约  
- C 端 7 页与 B 端视觉统一  
- 窝次 pupIdentityId 真机对齐  

## 开发 zip 用途

仅给内部审 UI / 代码，**不能**上传审核、不能给真用户。
