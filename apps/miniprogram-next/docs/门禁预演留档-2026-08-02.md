# 门禁预演留档 · 2026-08-02

在完整 monorepo（本机）执行，**非**源码 zip 沙箱。

## 1. `npm test`（vitest）

```
Test Files  21 passed | 1 skipped (22)
Tests       80 passed | 1 skipped (81)
```

**结果：通过**

## 2. `npm run test:native`（含 release_gate）

包含：

- `test/release_gate.test.mjs`（production 注入 / fail-closed / restore）
- 家系 / 微信登录 / 源码打包 / lint baseline 等

**结果：通过**（修掉 mp-ui / offline-dev 的 eslint error 后；lint warning 预算重冻结为 170，仅 any 告警）

## 3. 生产注入预演（scratch，不改工作区）

```bash
MP_TARGET_DIR=<scratch> \
MP_APP_ENV=production \
MP_API_BASE='https://api.scolv.example' \
MP_APPID='wx1234567890abcdef' \
MP_WECHAT_SUBSCRIBE_TEMPLATE_IDS='tmpl_demo_task,tmpl_demo_reservation' \
bash scripts/build-miniprogram-next.sh
```

注入后 config：

| 字段 | 值 |
| --- | --- |
| `APP_ENV` | `production` |
| `API_BASE` | `https://api.scolv.example` |
| `DEV_LOGIN_PHONE` | `''` |
| `DEV_LOGIN_CODE` | `''` |
| `RELEASE_INJECTED_AT` | 非空 ISO 时间（例 `2026-08-01T18:37:31Z`） |
| `WECHAT_SUBSCRIBE_TEMPLATE_IDS` | `tmpl_demo_task,tmpl_demo_reservation` |
| `DEFAULT_PUBLIC_SLUG` | `demo`（仍为默认，正式可另改） |

project.config：

- `appid` = 注入的 wx…
- `urlCheck` = `true`

`assertRuntimeConfig()`：**OK**

负例：`MP_API_BASE=https://p.scolv.com` → **exit 1**（staging 拒绝）

`--restore` 后 scratch 回到 `APP_ENV=development`。

**工作区** `apps/miniprogram-next/src/utils/config.js` **保持 development**，未污染。

## 4. 仍须人工（非本预演范围）

1. 正式库 `demo` 熊舍有货，或正式注入时改 `DEFAULT_PUBLIC_SLUG`
2. 用真实 `MP_API_BASE` / `MP_APPID` / 订阅模板再注入一次
3. `npm run build:weapp` 后上传 **dist**，勿用源码 zip

## 5. 说明

- 源码审阅 zip **不含** monorepo `scripts/build-miniprogram-next.sh`，沙箱跑不通 release_gate 属预期。
- 本预演在完整仓库完成，可作为哈德「门禁有证据」的本地留档。
