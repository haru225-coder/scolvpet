# 静态证据统计

- package 路径：2111
- 页面源码路径：205
- Controller 路径：32
- Service 路径：66
- Entity 路径：87
- API 路由字符串：250

生成时间：2026-07-15 23:15:55 +0800

## 来源与用途

- `page-paths.txt`：Flutter AOT 中恢复出的页面源码路径。
- `controller-paths.txt`：Flutter AOT 中恢复出的 Controller 路径。
- `service-paths.txt`：Flutter AOT 中恢复出的 data Service 路径。
- `entity-paths.txt`：Flutter AOT 中恢复出的 domain Entity 路径。
- `api-routes.txt`：Flutter AOT 中可见的 `/api/...` 路由字符串。
- `business-strings.txt`：繁育、健康、体重、提醒、谱系、会员和经营相关字符串集合。
- `web/`：公开访问的定价页、编辑器及其第一方生产 JavaScript，只用于静态字段和路由核验。
- `appstore/`：Apple 公开产品页、版本说明、宣传截图及 OCR 结果。
- `SHA256SUMS`：证据文件及本地 `App.framework/App` 的 SHA-256 清单。

本目录没有向目标业务接口提交请求，也没有包含账号、令牌或用户业务数据。
