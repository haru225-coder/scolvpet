# R3 测试夹具与账号策略

> 本轮 **未创建、未使用** 任何真实账号或可复用凭据。  
> 在启动阻断解除前，夹具仅定义规范，不落真实密钥。

## 原则

1. 所有写操作只允许使用本轮合成数据。
2. 证据目录禁止保存：Token、Cookie、验证码、手机号、真实客户/订单数据、生产 API 密钥。
3. 截图与日志入库前脱敏；设备 UDID 使用 `<REDACTED_UDID>`。

## 计划中的合成夹具（待 R3 解除 B 后启用）

| 夹具 ID | 用途 | 形态 |
|---|---|---|
| FIX-AUTH-01 | 未登录 | 干净安装，无 Keychain/Prefs 会话 |
| FIX-AUTH-02 | 已登录 | 测试手机号 + 一次性验证码（用后作废） |
| FIX-ORG-01 | 空组织 | 无猫/无客户 |
| FIX-ORG-02 | 最小繁育 | 2 成猫 + 1 计划（合成名） |
| FIX-OFFLINE-01 | 离线 | 设备飞行模式 / 断网后冷启动 |
| FIX-RESTART-01 | 重启 | 杀进程后冷启动，校验会话恢复 |

## 禁止事项

- 不得使用生产宠舍真实数据导出。
- 不得把 Bridge 注入的生产 Token 写入 `reverse-evidence/`。
- 不得为“跑通动态”而关闭证据等级标注或把 B 写成 D。
